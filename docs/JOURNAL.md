# Dev Notes

## Apr 2

Multiple templates require the same context because the footer displays live data
`utils.plugins` will contain a bunch of contextMixins to provide multiple templates with live data without repeating lines of code in `views.py`


